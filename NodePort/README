NodePort
На всех нодах кластера открываются порты
с диапазона 30000 - 30767
на всех нодах кластера,
при принятии трафика из вне,
трафик попадает на какую-либо ноду
а затем передается на ноду, где бежит
destin-приложение(целевое)
и потом передается на нужный под
Т.е NodePort используется для взаимодействия
кластера с внешним кластером/приложением